---
import Container from '@components/Container/Container'
import Title from '@ui/Title/Title'

interface Props {
  title: string
}

const { title } = Astro.props
---

<header class="header">
  <Container className={'header__container'}>
    <Title className="header__title" size="lg" text={title} />
  </Container>
</header>
